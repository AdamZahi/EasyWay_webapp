{% extends 'back-office/reclamation/backbase.html.twig' %}

{% block title %}Statistiques des Réclamations{% endblock %}

{% block content %}
{% block content %}
<div class="container mt-4">
    <h1 class="text-center mb-4">📊 Statistiques des Événements</h1>

    <div class="row">
        <div class="col-md-4">
            <div class="card text-white bg-primary mb-3 shadow-sm">
                <div class="card-body text-center">
                    <h4 class="card-title">Total Événements</h4>
                    <p class="display-4 fw-bold">{{ totalEvents }}</p>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card text-white bg-warning mb-3 shadow-sm">
                <div class="card-body text-center">
                    <h4 class="card-title">En attente</h4>
                    <p class="display-4 fw-bold">{{ statusCount['en_attente']|default(0) }}</p>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card text-white bg-success mb-3 shadow-sm">
                <div class="card-body text-center">
                    <h4 class="card-title">Terminé</h4>
                    <p class="display-4 fw-bold">{{ statusCount['Terminer']|default(0) }}</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Graphiques -->
    <div class="row">
        <div class="col-md-6">
            <div class="card shadow-sm p-3">
                <h4 class="text-center">Répartition des Événements par Statut</h4>
                <canvas id="statusChart"></canvas>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card shadow-sm p-3">
                <h4 class="text-center">Répartition par Type</h4>
                <canvas id="typeChart"></canvas>
            </div>
        </div>
    </div>

    <!-- Évolution des événements -->
    <div class="row mt-4">
        <div class="col-md-12">
            <div class="card shadow-sm p-3">
                <h4 class="text-center">Évolution des Événements</h4>
                <canvas id="evolutionChart"></canvas>
            </div>
        </div>
    </div>

    <div class="text-end mt-4 mb-3">
        <a href="{{ path('app_event_index') }}" class="btn btn-secondary">
            🔙 Retour à la liste des événements
        </a>
    </div>
</div>

<!-- Importation de Chart.js -->
<script src="https://cdnjs.cloudflare.com/ajax
{% endblock %}
