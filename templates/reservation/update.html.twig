{% extends 'front-office/front.html.twig' %}

{% block title %}Edit Reservation
{% endblock %}

{% block main_content %}
<br><br><br><br><br><br>
	<div class="container mt-4">
		<h1>Edit Reservation #{{ reservation.id }}</h1>

		{{ form_start(form) }}
		{{ form_widget(form) }}
		<button type="submit" class="btn btn-primary">Update</button>
		<a href="{{ path('reservation_list') }}" class="btn btn-secondary">Cancel</a>
		{{ form_end(form) }}
	</div>
{% endblock %}
